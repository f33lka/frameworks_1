# Docker-бандл для проекта **frameworks-main** (Flask + React/Vite)

Этот архив содержит **только** Docker-файлы и `docker-compose.yml`, плюс инструкцию по запуску и куда их вставить.

## Структура после распаковки архива

Распакуйте содержимое архива **в корень вашего проекта**, чтобы пути совпадали с существующей структурой:

```
<project-root>/
├─ backend/                 # уже существует в вашем проекте
│  ├─ Dockerfile            # (новый) из этого архива
│  └─ .dockerignore         # (новый) из этого архива
├─ frontend/                # уже существует в вашем проекте
│  ├─ Dockerfile            # (новый) из этого архива
│  ├─ .dockerignore         # (новый) из этого архива
│  └─ docker/
│     └─ nginx.conf         # (новый) из этого архива
└─ docker-compose.yml       # (новый) из этого архива
```

> **Важно:** в исходном репозитории уже есть `backend/.env.example`.
> Для продакшена скопируйте его в `backend/.env` и при необходимости отредактируйте значения (секреты).  
> По умолчанию база — SQLite в `instance/app.db`. Том `backend_data` сохраняет её между перезапусками контейнеров.

## Запуск

1) Из корня проекта (где лежит `docker-compose.yml`) выполните сборку образов:
```bash
docker compose build
```

2) Поднимите сервисы:
```bash
docker compose up -d
```

3) Откройте фронтенд в браузере:  
`http://localhost:8080`

- Бэкенд доступен внутри сети compose как `http://backend:5000` (Nginx проксирует `/api/*` на него).
- Предустановленный логин: **admin@example.com** / **admin123** (сидируется при первом запуске бэкенда).

### Полезные команды

```bash
# Логи
docker compose logs -f backend
docker compose logs -f frontend

# Пересборка после изменений исходников
docker compose build --no-cache && docker compose up -d

# Остановка и удаление контейнеров (без удаления томов с данными)
docker compose down

# Полная очистка (с удалением томов и БД SQLite)
docker compose down -v
```

## Где хранятся данные

- Файл БД SQLite (`instance/app.db`) хранится в Docker-томе `backend_data` и переживает перезапуски контейнера.
- Если хотите удалить данные — выполните `docker compose down -v`.

## Настройка CORS / API-URL

- Мы используем Nginx как **реверс-прокси**: фронт доступен на `:8080`, а API — на том же домене по пути `/api/*`.  
  Поэтому CORS дополнительно не требуется, но в бэкенде он включён и настроен на `*` по умолчанию.
- В сборке фронтенда используется `VITE_API_URL=/api` (прокси через Nginx). При необходимости можно изменить в `docker-compose.yml`:
  ```yaml
  frontend:
    build:
      args:
        - VITE_API_URL=/api   # поменяйте при желании
  ```

## Частые вопросы

- **Где лежать Docker-файлы?** — Вложите их строго по путям, указанным в дереве выше. Файлы `Dockerfile` должны находиться в директориях соответствующих приложений (`backend/` и `frontend/`).  
- **Можно ли использовать локальную .env?** — Да. Скопируйте `backend/.env.example` в `backend/.env` и подставьте свои значения `SECRET_KEY`, `JWT_SECRET_KEY`, `CORS_ORIGINS` и т.д.
- **Нужна ли внешняя БД?** — Нет. По умолчанию используется SQLite. При желании можете заменить `DATABASE_URL` на PostgreSQL/MySQL и добавить сервис в `docker-compose.yml`.

Удачного запуска! 🚀
